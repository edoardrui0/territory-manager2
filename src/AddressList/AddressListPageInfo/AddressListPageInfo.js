import React, { Component } from "react";

export default class AddressListPageInfo extends Component {
  render() {
    return (
      <div>
        <p>Addresses</p>
        <p>Select an Address to view its details</p>
      </div>
    );
  }
}
